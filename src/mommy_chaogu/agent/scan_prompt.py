"""Agent 盘中扫描专用 prompt。

与 chat / report prompt 不同，这个 prompt 要求 LLM 返回结构化 JSON，
而不是自然语言文本。
"""

SCAN_PROMPT_TEMPLATE = """\
以下是用户自选股的实时行情和资金流数据：

{data}

请判断：有没有需要立即提醒用户的异动？

判断标准（参考，结合语境灵活把握，不是硬规则）：
- 涨跌幅 > 7% 或 < -5%（但大盘暴跌时 -5% 是正常的，不推）
- 主力净流入/流出占流通市值 > 5bp 且方向与股价一致
- 量比 > 2 且涨幅 > 3%（放量突破）或 跌幅 > 2%（放量下杀）
- 换手率突然飙升（> 8%）
- 涨停/跌停（但这类硬告警已由系统自动推，你不需要重复推）

如果有需要推的，返回 JSON：
{{"alerts": [{{"code": "600519", "name": "贵州茅台", "severity": "warning", "message": "主力净流入 3.2 亿，放量突破，量比 2.8"}}]}}

severity 取值：warning（需要关注）、critical（建议行动）

如果今天没有需要推的，返回：
{{"alerts": [], "summary": "今天行情平淡，无需提醒"}}

只返回 JSON，不要多余文字。
"""
