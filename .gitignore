# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# uv / virtualenvs
.venv/
venv/
env/
.python-version

# 测试 / 覆盖率
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.json
coverage.xml
output/
reports/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# 日志 / 临时
*.log
*.tmp
.env
.env.local

# 行情数据缓存（未来加的话）
data/cache/
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
data/*.bak
data/*.sqlite
data/monitor.log
data/.flow_monitor_state.json
data/.cache/
data/flows_monitor.log
data/quote_snapshot.json
data/quote_innovative_drug.json
data/flows_report_*.md
# 排除以上运行时文件，但保留 supply_chains/ 供应链数据资产（仅静态种子）
!data/supply_chains/


node_modules/

# 本地实验数据库归档（bt_* 回测 / eval-* 实验，2026-08-22 从 data/ 顶层迁入）
data/archive/

# ZCode 本机配置（含个人绝对路径 / 软链，不入仓）
.zcode/config.json
# 仓库内 dev 副本：market-watch-loop 以 gitignore 排除（同步进 bundled_skills/ 发布）；
# market-monitoring-test 是纯实验目录，走 pyproject.toml 的 wheel exclude
# （见 tests/test_release_consistency.py 的 _EXCLUDED_SKILL_WHITELIST 白名单）——
# 两套机制分工不同，新增本地 Skill 副本时参照此说明选择。
.agents/skills/market-watch-loop
