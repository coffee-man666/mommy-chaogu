[project]
name = "mommy-chaogu"
dynamic = ["version"]
description = "可由现有 Agent 接管的本地 A 股 / 美股投研与监控应用"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "coffee-man666" }
]
dependencies = [
    "efinance>=0.5.8",
    "pandas>=2.2",
    "requests>=2.32",
    "sqlalchemy>=2.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "websockets>=13",
    "jinja2>=3.1.6",
    "openai>=1.0",
    "mcp>=1.28.1",
    "prompt-toolkit>=3.0.51",
    "sqlite-vec>=0.1.9",
    "python-dotenv>=1.0",
    "rich>=13.9",
    "textual>=8.2.8",
    "qrcode>=8.0",
    "cryptography>=50.0.0",
]

[project.scripts]
mommy-watchlist = "mommy_chaogu.cli:main_watchlist"
mommy-monitor = "mommy_chaogu.cli:main_monitor"
mommy-report = "mommy_chaogu.cli:main_report"
mommy-cache = "mommy_chaogu.cli:main_cache"
mommy-semicon = "mommy_chaogu.cli:main_semicon"
mommy-flows = "mommy_chaogu.cli:main_flows"
mommy-web = "mommy_chaogu.cli:main_web"
mommy-earnings = "mommy_chaogu.earnings.cli:main_earnings"
mommy-agent = "mommy_chaogu.cli:main_agent"
mommy-memory = "mommy_chaogu.cli:main_memory"
mommy-mcp = "mommy_chaogu.agent.mcp_server:main_mcp"
mommy-chaogu = "mommy_chaogu.cli:main"
mommy = "mommy_chaogu.cli:main_mommy"
mommy-tui = "mommy_chaogu.tui.app:main"
mommy-channel = "mommy_chaogu.cli:main_channel"
mommy-connect = "mommy_chaogu.cli:main_connect"
mommy-doctor = "mommy_chaogu.cli:main_doctor"
mommy-setup = "mommy_chaogu.setup:main_setup"

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-cov>=5",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-requests",
    "httpx>=0.27",  # FastAPI 测试客户端
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
# Runtime output must never enter a release artifact; market-monitoring-test is a
# local experimental Skill, not a bundled product Skill. All six catalog plugins
# under bundled_skills/ are part of the installable project package.
exclude = [
    "/output/**",
    "/src/mommy_chaogu/bundled_skills/market-monitoring-test/**",
]

[tool.hatch.build.targets.sdist]
# Hatch's sdist file selection follows repository ignore rules. Keep the
# catalog plugins explicit so wheel builds from sdist remain complete.
force-include = {"src/mommy_chaogu/bundled_skills/mommy-onboard" = "src/mommy_chaogu/bundled_skills/mommy-onboard", "src/mommy_chaogu/bundled_skills/mommy-research" = "src/mommy_chaogu/bundled_skills/mommy-research", "src/mommy_chaogu/bundled_skills/mommy-strategy" = "src/mommy_chaogu/bundled_skills/mommy-strategy", "src/mommy_chaogu/bundled_skills/market-watch-loop" = "src/mommy_chaogu/bundled_skills/market-watch-loop", "src/mommy_chaogu/bundled_skills/basket-analysis" = "src/mommy_chaogu/bundled_skills/basket-analysis", "src/mommy_chaogu/bundled_skills/food-security-analysis" = "src/mommy_chaogu/bundled_skills/food-security-analysis"}

[tool.hatch.build.targets.wheel]
packages = ["src/mommy_chaogu"]

[tool.hatch.version]
path = "src/mommy_chaogu/version.py"

[tool.ruff]
line-length = 100
target-version = "py312"
# These two directories are independently versioned, distributable Skill
# payloads from the public Plugins Store; their scripts are not application
# modules and should be linted by their own release process.
exclude = [
    "src/mommy_chaogu/bundled_skills/basket-analysis/**",
    "src/mommy_chaogu/bundled_skills/food-security-analysis/**",
    "site/install-skill.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "RUF"]
# E501: line too long (允许多行 dataclass 字段长)
# RUF003: ambiguous unicode characters — 中文 docstring/注释必然含全角符号
ignore = ["E501", "RUF001", "RUF002", "RUF003"]

[tool.mypy]
strict = true
python_version = "3.12"
# Store payload scripts are standalone Skill assets, not imported project code.
exclude = "src/mommy_chaogu/bundled_skills/(basket-analysis|food-security-analysis)/"

[[tool.mypy.overrides]]
module = [
    "efinance", "pandas", "pandas.*", "rich", "retry", "tqdm", "multitasking",
    "jsonpath", "requests", "requests.*",
    "qrcode", "qrcode.*",
    "sqlite_vec", "tiktoken",
    "sqlalchemy", "sqlalchemy.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["fastapi", "uvicorn", "starlette", "httpx", "openai", "openai.*", "mcp", "mcp.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"
markers = [
    "network: requires live efinance API access (东财接口)",
]

[tool.coverage.run]
branch = true
source = ["mommy_chaogu"]

[tool.coverage.report]
show_missing = true
skip_covered = true
precision = 1
fail_under = 70
